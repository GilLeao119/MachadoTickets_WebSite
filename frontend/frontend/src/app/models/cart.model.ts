export interface Cart {
    items: Array<CartItem>;
  }
  
  export interface CartItem {
    NomeEvento: string;
    price: number;
    quantityk: number;
    quantitya: number;
    quantityo: number;
    id: string;
  }
  